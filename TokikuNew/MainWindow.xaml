<Window x:Class="TokikuNew.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Tokiku.ViewModels;assembly=Tokiku.ViewModels"
        xmlns:local="clr-namespace:TokikuNew"
        mc:Ignorable="d"
        Title="東菊金屬 ERP System" Height="350" Width="792.685" WindowState="Maximized" Icon="16x16.png" Loaded="Window_Loaded">
    <Window.Resources>
        <vm:MainViewModel x:Key="MainModel" x:Name="Model" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="22"/>
            <RowDefinition Height="*"/>
            
            <RowDefinition Height="27"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" FontSize="17">
            <MenuItem Header="專案" Name="MI_Project" Click="MI_Project_Click">
            </MenuItem>
            <MenuItem Header="主檔建立" FontSize="17" Name="MI_CreateNew">
                <MenuItem Header="廠商" Name="MI_CreateNew_Factories" Click="MI_CreateNew_Factories_Click"></MenuItem>
                <MenuItem Header="客戶" Name="MI_CreateNew_Customers" Click="MI_CreateNew_Customers_Click"></MenuItem>
                <MenuItem Header="聯絡人" Name="MI_CreateNew_Contracts" Click="MI_CreateNew_Contracts_Click"></MenuItem>
                <MenuItem Header="專案" Name="MI_CreateNew_Project" Click="MI_CreateNew_Project_Click"></MenuItem>
            </MenuItem>
          
            <MenuItem Header="報表"></MenuItem>
            <MenuItem Header="帳務"></MenuItem>
            <MenuItem Header="系統管理" FontSize="17">
                <MenuItem Header="人員管理" ></MenuItem>
                <Separator/>
                <MenuItem Header="結束程式" Name="ExitApp" Click="ExitApp_Click"></MenuItem>
            </MenuItem>
        </Menu>
        <TabControl Grid.Row="1" Margin="0,0,0,0" x:Name="Workspaces">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="Black" CornerRadius="4,4,0,0" Margin="2,0">
                                    <StackPanel Orientation="Horizontal" x:Name="TitleBlock" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,2">
                                        <TextBlock Text="{Binding Header, RelativeSource={RelativeSource TemplatedParent}}"></TextBlock>
                                        <Button x:Name="btnTabClose" Margin="5,0,0,0" FontSize="9" Width="12" Height="12" Content="X" BorderThickness="0" Background="Transparent" Click="btnTabClose_Click" ></Button>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="LightSkyBlue"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Header="d">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="TabItem">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>
       
        <StatusBar Margin="0" Grid.Row="3">
            <StackPanel Orientation="Horizontal">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xe187;" />
                <TextBlock Name="userNameDisplay" Text="{Binding LoginedUser.UserName, Source={StaticResource MainModel}}"/>
            </StackPanel>
        </StatusBar>
    </Grid>
</Window>
