<UserControl x:Class="TokikuNew.Views.RequiredListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TokikuNew.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1024" Loaded="RequiredListView_Loaded">
    <Grid>
        <DataGrid x:Name="CheckGrid" AutoGenerateColumns="False" Margin="5" ItemsSource="{Binding}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="需求單號" Width="auto" />
                <DataGridTextColumn Header="廠商編號" Width="auto" />
                <DataGridTextColumn Header="廠商名稱" Width="auto" />
                <DataGridTextColumn Header="建單日期" Width="auto" />
                <DataGridTextColumn Header="建單人員" Width="auto" />
            </DataGrid.Columns>
            <DataGridTemplateColumn>
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Content="開啟需求單" x:Name="BtnOpen" Click="BtnOpen_Click" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid>
            
    </Grid>
</UserControl>
