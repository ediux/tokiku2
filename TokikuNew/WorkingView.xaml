<Window x:Class="TokikuNew.WorkingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:TokikuNew.Controls"
        xmlns:local="clr-namespace:TokikuNew"
        mc:Ignorable="d"
        Title="加工圖集" Height="315" Width="554">
    <Grid>
        <GroupBox Grid.Column="0" Grid.Row="0"  Grid.ColumnSpan="2" Margin="5" Header="加工圖集建置">
            <Controls:CustomDataGrid Margin="5,6,5,4" VerticalAlignment="Stretch" 
                                         AllowExecuteSystemCommand="True"
                                         AutoGenerateColumns="False">
                <Controls:CustomDataGrid.Columns>
                    <DataGridTextColumn Header="圖集"/>
                    <DataGridTextColumn Header="名稱"/>
                    <DataGridTextColumn Header="更新次數"/>
                    <DataGridTextColumn Header="最新日期"/>
                    <DataGridTextColumn Header="施工順序異動日期"/>
                </Controls:CustomDataGrid.Columns>
            </Controls:CustomDataGrid>
        </GroupBox>
    </Grid>
</Window>
